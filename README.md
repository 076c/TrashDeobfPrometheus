code is pretty shit and pretty unreadable

it can unwrap the main function and resolve parameters and offsets (propagating fenv globals isn't implemented yet but I tried to..)

anyways this is getting rewritten in C# and this code is trash and incomplete I tried to reverse the prometheus compiler in order but no guarantees
